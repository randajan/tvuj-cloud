import React from 'react';
import { Block } from "@randajan/jet-react/dom/block";

import "./{{pascalCase name}}.scss";

export const {{pascalCase name}} = (props)=>{
    const {} = props;
    
    return (
        <Block className="{{pascalCase name}}" caption="{{name}}">
            
        </Block>
    )
}

